1.项目开发遵循java开发规范。

2.项目编码UTF-8。

3.项目框架jdk1.6+Spring(3.1)MVC+hibernate3.5+mysql(5.20)。

4.jar包说明：项目中需要添加jar,必须在此说明jar的用途
					antlr-2.76.jar -----------------hibernate中的语法分析用。
					aopalliance-1.0.jar-------------spring中AOP依赖包。
					aspectjweaver.jar---------------spring中AOP依赖包。
					asm.jar-------------------------操作java字节码的类库。
					asm-attrs.jar-------------------操作java字节码的类库。
					c3p0-0.8.5.2.jar----------------数据库连接池。
					cglib-2.1.jar-------------------运行时代码生成库，动态代理类。
					cleanimports.jar----------------清理类中无用的导入包。
					commons*.jar-----------------------阿帕奇工具包。
					concurrent-1.3.2.jar------------Doug Lea’s的并发编程工具。
					connector.jar-------------------Sun集成企业系统规范。
					dom4j-1.6.1.jar-----------------用于简化XML，Xpath，XSLT使用的库。
					ehcache-1.1.jar-----------------用于规范Hibernate在内存与磁盘缓冲的使用。
					hibernate*----------------------hibernate相关包。
					itext.jar-----------------------pdf类库。
					jaas.jar------------------------Java身份验证和授权服务。
					jacc-1_0-fr.jar-----------------hibernate依赖包。
					validation-api-1.0.0.GA.jar-----验证。
					javassist-3.9.0.GA.jar----------hibernate依赖包。
					jackson-all-1.8.1.jar-----------高效的json包。
					jakarta-oro.jar-----------------全面以及优化得最好的正则表达式API之一。
					jdbc2_0-stdext.jar--------------JDBC2.0可选包，服务器端数据库。
					jdom.jar------------------------操作XML类库。
					jgroups-2.2.7.jar---------------分布式消息 相关。
					jta-1.1.jar---------------------jta事务。
					junit.jar-----------------------单元测试。
					jxl.jar-------------------------操作EXCEL。
					log4j-1.2.14.jar----------------日志类库。
					mysqlconnectorjava5.0.jar-------mysql5驱动。
					nekohtml-1.9.6.jar--------------解析HTML类库。
					org.springframework*.jar------------spring类库。
					oscache-2.1.jar-----------------缓存类库。
					PDFBox-0.7.3.jar----------------PDF类库。
					poi*----------------------------操作MS-EXCEL，WORD,pdf类库。
					proxool-0.8.3.jar---------------连接池。
					sitemesh-2.2.1.jar--------------JSP页面布局。
					slf4j*.jar----------------------日志接口类库。
					standard-1.1.2.jar--------------jstl需要的jar
					swarmcache-1.0rc2.jar-----------分布式缓存。
					xerces-2.6.2.jar----------------XML解析器。
					xml-apis.jar--------------------JAXP支持。
					xercesImpl.jar------------------xerces-2.6.2.jar依赖包。
					xmlParserAPIs.jar---------------xml解析。
					xmlbeans-2.3.0------------------poi-ooxml*.jar依赖包。
					geronimo-stax-api_1.0_spec-1.0.jar--poi-ooxml*.jar依赖包。
					jrobin-1.5.9.1.jar------------------监控系统运行情况使用的jar
					jira-javamelody.jar-----------------监控系统运行情况使用的jar
	
	
	
	
	
	
	